using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AccelerateController
{
    public void Accelerate(Car car)
    {
        //car.speed += car.acceleration * Time.deltaTime;
        //Debug.Log($"{car} is accelerating");
    }
}
